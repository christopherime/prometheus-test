groups:
  - name: node_exporter
    rules:
      - alert: AlerteBidon
        expr: windows_cs_hostname != 0
        labels:
          severity: critical
        annotations:
          summary: "Alerte Vraiment Bidon"